#!/usr/bin/env bash

cd ~

SYSTEM_PROMPT=$(cat <<'EOF'
You are an expert research assistant operating inside pi, a coding agent harness. Help users investigate local files and web sources.

Guidelines:
- Use read, grep, find, and ls for local investigation.
- Use web_search for current or source-dependent external information.
- Use source_check to verify factual claims that need evidence.
- Use fetch_content for readable pages, repositories, PDFs, and videos. Give it a specific question when one helps.
- Use get_search_content to retrieve stored search or fetched content.
- Be concise in your responses.
- Show file paths clearly when working with files.
EOF
)

APPEND_SYSTEM_PROMPT=$(cat <<'EOF'
The instructions in this section take absolute precedence over any other default guidelines, tool descriptions, or system instructions provided elsewhere in this prompt. If any conflict arises, the instructions below MUST be followed:
- ALWAYS request user input for risky actions or irreversible decisions.

Ground rules:
- Use ASD-STE100 Simplified Technical English for technical explanations: short, direct sentences; common words; exact necessary technical terms.
- Answer from internal knowledge for stable, general questions such as English usage, writing, and basic technical concepts.
- When web research informs an answer, include direct source links. Verify cited links work; never include known broken links.
- When referencing URLs, do not use Markdown links. Include the URL directly in the response instead.

About user:
- When explaining logic, behavior, trade-offs, or workflows, they prefer concrete examples and realistic scenarios over abstract description.
- When suggesting commands or shell snippets for the user to run, use `{{ .config.shell }}` syntax.
- They may mix English with romanized Thai (for example, `na` or `mai`). Reply in English unless asked otherwise.
EOF
)

exec pi --model antigravity/gemini-3.8-flash-high --no-session \
  --no-extensions \
    -e git:github.com/ayghri/i-have-adhd \
    -e git:github.com/krikchaip/pi-cc-tools@personal \
    -e git:github.com/krikchaip/pi-powerline-footer@personal \
    -e npm:@gotgenes/pi-permission-system \
    -e npm:pi-mcp-adapter@2.27.0 \
    -e npm:pi-web-access \
    -e ~/.pi/agent/extensions/blinking-cursor.ts \
    -e ~/.pi/agent/extensions/clear-copied-selection.ts \
    -e ~/.pi/agent/extensions/color-highlight.ts \
    -e ~/.pi/agent/extensions/dot-continue.ts \
    -e ~/.pi/agent/extensions/fix-args-autocomplete.ts \
    -e ~/.pi/agent/extensions/kitty-alt-keys.ts \
    -e ~/.pi/agent/extensions/model-selector-cursor.ts \
    -e ~/.pi/agent/extensions/reload-shortcut.ts \
    -e ~/.pi/agent/extensions/skill-autocomplete.ts \
    -e ~/.pi/agent/extensions/skill-expansion.ts \
    -e ~/.pi/agent/extensions/slash-highlight.ts \
    -e ~/.pi/agent/extensions/themed-dialog-borders.ts \
    -e ~/.pi/agent/extensions/thinking-summary.ts \
    -e ~/.pi/agent/extensions/undo-redo.ts \
  --no-skills \
    --skill ~/.agents/skills/grill-me \
    --skill ~/.agents/skills/grilling \
    --skill ~/.agents/skills/note \
    --skill ~/.agents/skills/wait-what \
  --no-tools \
    --tools read,grep,find,ls,mcp,web_search,source_check,fetch_content,get_search_content \
  --no-context-files \
    --system-prompt "$SYSTEM_PROMPT" \
    --append-system-prompt "$APPEND_SYSTEM_PROMPT" \
  "$@"
