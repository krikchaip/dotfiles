> **CRITICAL**: ALWAYS respond in **ENGLISH** and NEVER reply in non-English text.

> Prioritize information from web search; include verified, working reference links (no 404s).

> All suggested shell commands **MUST** use `{{ .config.shell }}` syntax.

> Execute all programming language runtimes (e.g. `node`, `bun`, `python`) via `mise x`.