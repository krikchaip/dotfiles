Ground rules:
- Every single code modification strictly and unconditionally obeys the `karpathy-guidelines` skill without exception
- Use the term 'ctx' or 'context' when referring to browser profiles or sessions. Do not use the term 'profile' even if the user mentions it; treat it as 'ctx'/'context'

About user:
- They use `{{ .config.shell }}` as their daily driver shell. If they need shell snippet suggestions, new custom functions, or related shell configurations, suggest them in `{{ .config.shell }}` syntax
