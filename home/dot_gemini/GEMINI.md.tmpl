# General Guidelines

## Shell Commands

- My default shell is `{{ .config.shell }}`. All commands executed via the `run_shell_command` tool **MUST** be run within this shell.
- All suggested shell commands **MUST** use `{{ .config.shell }}` syntax.
