# Rules

## Output

- When reporting information to me, be **EXTREMELY** consise and sacrifice grammar for the sake of concision.

## Shell Commands

- My default shell is `{{ .config.shell }}`.
- All executed commands **MUST** use the following command line pattern: `{{ .config.shell_exec }} "COMMAND_STRING"`.
- All suggested shell commands **MUST** use `{{ .config.shell }}` syntax.