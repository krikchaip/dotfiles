# `krikchaip`'s neovim configs

## One-line install

```sh
# any command can have a bang(!) to make the command wait till it finished
nvim --headless '+Lazy! restore' '+MasonToolsInstallSync' +qa
```
